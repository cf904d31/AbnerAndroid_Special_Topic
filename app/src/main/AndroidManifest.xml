<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.specialtopic3">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FarmChooseActivity" />
        <activity android:name=".TownChooseActivity" />
        <activity android:name=".LoginFarmDetial" />
        <activity android:name=".EducationTownChoose" />
        <activity android:name=".LiveTownChoose" />
        <activity android:name=".EducationMainActivity" />
        <activity android:name=".LiveMainActivity" />
        <activity android:name=".EducationFarmDetial" />
        <activity android:name=".LiveFarmDetial"></activity>
    </application>

</manifest>